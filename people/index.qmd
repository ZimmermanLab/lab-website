---
title: "People"
description: |
  Current and past members of the Zimmerman Lab at USF.
listing:
  - id: pi
    max-description-length: 500
    image-height: 150px
    fields: [image, title, description]
    contents: 
      - naupaka_zimmerman
    type: grid
    grid-item-border: false
    grid-columns: 2

  - id: current_ug_students
    max-description-length: 500
    image-height: 150px
    fields: [image, title, description]
    contents: 
      - ug/ava_albert
    type: grid
    grid-item-border: false
    grid-columns: 2

  - id: alum_grad_students
    max-description-length: 500
    image-height: 150px
    fields: [image, title, description]
    contents: 
      - grad/sarah_gao
      - grad/ashley_sango
      - grad/jason_krastins
      - grad/derek_newberger
      - grad/joshua_copeland
    type: grid
    grid-item-border: false
    grid-columns: 2 

  - id: alum_ug_students
    max-description-length: 500
    image-height: 150px
    fields: [image, title, description]
    contents: 
      - ug/natalie_ashburner_wright
      - ug/christina_tran
      - ug/emma_gibson
    type: grid
    grid-item-border: false
    grid-columns: 2

  - id: alum_hs_students
    max-description-length: 500
    image-height: 150px
    fields: [image, title, description]
    contents: 
      - hs/amirtha_maria
    type: grid
    grid-item-border: false
    grid-columns: 2
---
![Pre-COVID Lab Group Meeting](lab.jpg)

## PI

This is an example of **grid** listing. You can specify the content in the `yaml` header in the `.qmd` file. 

It is convenient to use `id` to link to multiple folders. 

::: {#pi}
:::

## Current Undergraduate Students

This is an example of **grid** listing. You can specify the content in the `yaml` header in the `.qmd` file. 

It is convenient to use `id` to link to multiple folders. 

::: {#current_ug_students}
:::

## Alumni

### Graduate Students

This is an example of **grid** listing. You can specify the content in the `yaml` header in the `.qmd` file. 

It is convenient to use `id` to link to multiple folders. 

::: {#alum_grad_students}
:::

### Undergraduate Students

This is an example of **grid** listing. You can specify the content in the `yaml` header in the `.qmd` file. 

It is convenient to use `id` to link to multiple folders. 

::: {#alum_ug_students}
:::

### High School Students

This is an example of **grid** listing. You can specify the content in the `yaml` header in the `.qmd` file. 

It is convenient to use `id` to link to multiple folders. 

::: {#alum_hs_students}
:::
